# ============================================================
# CineScope — 统一依赖
# 合并自 etl / recommender / training / server 各子模块
# ============================================================

# --- 数据处理（etl / recommender / server 共用）---
pandas>=2.0,<4.0
numpy>=1.24
requests>=2.31,<3.0

# --- 机器学习 / 训练（training）---
xgboost>=2.0
lightgbm>=4.0

# --- 推荐（recommender）---
scipy>=1.10
scikit-learn>=1.3
joblib>=1.3

# --- Web 服务（recommender / server 共用）---
fastapi>=0.100
uvicorn>=0.23
pydantic>=2.0

# --- 后端（server）---
httpx>=0.25
python-dotenv>=1.0
